[package]
name = "laya-pstr-sampler"
version = "0.1.0"
edition = "2024"
publish = false
license = "Apache-2.0"
description = "Read-only SMC PSTR power samples for local Laya energy benchmarks"

[[bin]]
name = "pstr-sampler"
path = "src/main.rs"

[dependencies]
macmon = { version = "=0.8.2", default-features = false }
